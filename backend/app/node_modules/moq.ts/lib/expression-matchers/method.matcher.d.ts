import { ArgumentsMatcher } from "./arguments.matcher";
import { MethodInteraction } from "../interactions";
import { ExpectedMethodExpression } from "../expected-expressions/expected-expressions";
import { It } from "../expected-expressions/expression-predicates";
/**
 * @hidden
 */
export declare class MethodExpressionMatcher {
    private argumentsMatcher;
    constructor(argumentsMatcher: ArgumentsMatcher);
    matched(left: MethodInteraction, right: ExpectedMethodExpression | It<any>): boolean;
}
