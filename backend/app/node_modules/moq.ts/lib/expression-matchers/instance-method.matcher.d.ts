import { ArgumentsMatcher } from "./arguments.matcher";
import { NamedMethodInteraction } from "../interactions";
import { ExpectedNamedMethodExpression } from "../expected-expressions/expected-expressions";
import { It } from "../expected-expressions/expression-predicates";
/**
 * @hidden
 */
export declare class NamedMethodExpressionMatcher {
    private argumentsMatcher;
    constructor(argumentsMatcher: ArgumentsMatcher);
    matched(left: NamedMethodInteraction, right: ExpectedNamedMethodExpression | It<any>): boolean;
}
