import { IPreset } from "./preset";
import { ExpectedExpressions } from "../../expected-expressions/expected-expressions";
import { Interaction } from "../../interactions";
import { IPlayable } from "../../moq";
export declare class CallbacksPreset<T> implements IPreset<T> {
    readonly playable: IPlayable;
    readonly target: ExpectedExpressions<T>;
    readonly callback: (interaction: Interaction) => unknown;
    constructor(playable: IPlayable, target: ExpectedExpressions<T>, callback: (interaction: Interaction) => unknown);
}
