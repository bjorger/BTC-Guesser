import { It } from "../expected-expressions/expression-predicates";
import { EqualMatcher } from "./equal.matcher";
/**
 * @hidden
 */
export declare class EqualConstantMatcher {
    private equalMatcher;
    constructor(equalMatcher: EqualMatcher);
    matched(left: any, right: any | It<any>): boolean;
}
