import { Interaction } from "../interactions";
/**
 * @hidden
 */
export declare class CallbackPresetPlayer {
    play<TValue>(callback: (interaction: Interaction) => TValue, interaction: Interaction): any;
}
