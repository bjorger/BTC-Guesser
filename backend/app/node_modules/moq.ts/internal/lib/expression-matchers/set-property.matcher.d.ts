import { ExpectedSetPropertyExpression } from "../expected-expressions/expected-expressions";
import { SetPropertyInteraction } from "../interactions";
import { It } from "../expected-expressions/expression-predicates";
import { ConstantMatcher } from "./constant.matcher";
/**
 * @hidden
 */
export declare class SetPropertyExpressionMatcher {
    private constantMatcher;
    constructor(constantMatcher: ConstantMatcher);
    matched(left: SetPropertyInteraction, right: ExpectedSetPropertyExpression | It<any>): boolean;
}
