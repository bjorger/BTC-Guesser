import { ExpectedExpressions } from "../expected-expressions/expected-expressions";
import { ExpressionFormatter } from "./expression-formatter";
/**
 * @hidden
 */
export declare class ExpectedExpressionFormatter {
    private expressionFormatter;
    constructor(expressionFormatter: ExpressionFormatter);
    format(expected: ExpectedExpressions<any>, timesMessage: string, haveBeenCalledTimes: number, mockName?: string): string;
}
