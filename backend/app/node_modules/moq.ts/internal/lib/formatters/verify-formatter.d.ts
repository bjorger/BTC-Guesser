import { ExpectedExpressions } from "../expected-expressions/expected-expressions";
import { Interaction } from "../interactions";
import { ExpectedExpressionFormatter } from "./expected-expression-formatter";
import { TrackedExpressionsFormatter } from "./tracked-expressions-formatter";
/**
 * @hidden
 */
export declare class VerifyFormatter {
    private expectedExpressionFormatter;
    private trackedExpressionsFormatter;
    constructor(expectedExpressionFormatter: ExpectedExpressionFormatter, trackedExpressionsFormatter: TrackedExpressionsFormatter);
    format(expected: ExpectedExpressions<any>, timesMessage: string, haveBeenCalledTimes: number, trackedExpressions: Interaction[], mockName?: string): string;
}
