import { IPreset } from "../presets/presets/preset";
import { Interaction } from "../interactions";
import { ExpressionMatcher } from "../expression-matchers/expression.matcher";
import { Presets } from "../presets/presets";
/**
 * @hidden
 */
export declare class PresetPlayablesUpdater {
    private presets;
    private matcher;
    constructor(presets: Presets<unknown>, matcher: ExpressionMatcher);
    update(interaction: Interaction, playable: IPreset<unknown>): void;
}
